{
  "lerna": "2.9.0",
  "packages": [
    "packages/*"
  ],
  "version": "0.0.0",
  "scripts": {
    "postinstall": "lerna bootstrap && lerna run link",
    "dev": "lerna run --parallel dev",
    "build": "lerna run --parallel build"
  }
}
